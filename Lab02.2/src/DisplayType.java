/*
 *  This provides for exactly 5 instances of DisplayType
 *
 *  These are DisplayType objects, NOT Strings
 *  They are referenced as:
 *    DisplayType.LED...DisplayType.CRT  (5) total
 *
 */

enum DisplayType {
   LED, OLED, PLASMA, LCD, CRT
}